server:
  port: 1001
spring:
  application:
    name: springboot_demo
master:
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driverClassName: com.mysql.jdbc.Driver
second:
  datasource:
    url: jdbc:oracle:thin:@172.18.1.3:1521:orcl
    username: dpmsdb
    password: dpmsdb123
    driverClassName: oracle.jdbc.driver.OracleDriver

mybatis:
  mapper-locations: classpath*:/mapper/*.xml

## 日志级别设置
#logging:
#  level:
#    com.fanciter: DEBUG